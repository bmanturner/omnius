{
  "name": "example-rust-service",
  "private": true,
  "packageManager": "pnpm@11.23.0",
  "engines": {
    "node": "24.19.0"
  },
  "scripts": {
    "contracts:generate": "cargo xtask contracts generate && pnpm --filter @service/web-sdk generate",
    "contracts:check": "cargo xtask contracts check && pnpm --filter @service/web-sdk check:generated",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "test:e2e": "playwright test",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "typescript": "6.0.2",
    "orval": "8.26.0",
    "vite": "8.2.2",
    "vitest": "4.1.11",
    "@playwright/test": "1.62.1"
  }
}
